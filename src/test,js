// const Heap = require("js-heap");

// const heap = new Heap((a, b) => a - b);

// heap.push(5);
// heap.push(3);
// heap.push(8);
// heap.push(1);

// heap.print(); // Nên thấy 1 ở đầu heap

// console.log(heap.pop()); // Nên in ra 1
// heap.print(); // Nên thấy 3 ở đầu heap

// console.log(heap.pop()); // Nên in ra 3
// heap.print(); // Nên thấy 5 ở đầu heap

// console.log("log");
